import SwipeCarousel from './swipe-carousel.js';

const carousel = new SwipeCarousel({
  // containerID: '.mySlider',
  // slideID: '.mySlide',
  // interval: 5000,
  // isPlaying: true
});
carousel.init();
